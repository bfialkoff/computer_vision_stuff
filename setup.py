from setuptools import setup

setup(
   name='cv_stuff',
   version='1.0',
   description='A useful module',
   author='B F',
   author_email='foomail@foo.com',
   packages=['cv_stuff'],  #same as name
   )
